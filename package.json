{
  "private": true,
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm ci --omit=dev && npx prisma generate && npx prisma migrate deploy"
  },
  "engines": {
    "node": "22.x"
  }
}
