{
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install && npm run build"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "socket.io-client": "^4.8.3"
  }
}
